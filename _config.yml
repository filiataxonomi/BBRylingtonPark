title: Rylington Park — The Public Record
sitename: Rylington Park — Boyup Brook — Public Record

# Every page automatically uses the design in _layouts/default.html.
# You never need to mention the layout in your page files.
defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - README.md
